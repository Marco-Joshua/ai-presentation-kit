# AGENTS.md — 이 폴더를 연 AI에게

이 폴더는 **자료를 넣으면 발표 자료(장표·PDF·내레이션 영상)가 나오는 키트**입니다.
사용자가 무엇을 해달라고 하기 전에, 아래 지도를 먼저 이해하세요.

## 폴더 지도

| 위치 | 역할 |
|---|---|
| `refs/날짜/` | 사용자가 넣는 원문 — 회의 전사·메일·메모·사진·스크린샷. **수정 금지** |
| `PROJECT_STATE.md` | 승인된 현재 상태. 사람이 승인한 것만 반영 |
| `01_읽기.md` `02_만들기.md` `03_검사.md` `04_영상.md` | 단계별 작업 지침. 해당 단계에서 반드시 읽을 것 |
| `장표_템플릿.html` | **장표는 이 파일의 스타일·구성요소를 그대로 쓰고 내용만 바꾼다** |
| `assets/` | 이미지. 스타일 규격은 `assets/이미지_스타일.md` |
| `scripts/make_video.py` | 장표+장면.json → 내레이션·자막 MP4 |
| `output/` | 산출물 저장 위치 |

## 기본 작업 흐름

사용자가 "발표 자료 만들어줘"라고 하면 이 순서를 따릅니다.

1. **읽기** — `01_읽기.md`대로 PROJECT_STATE와 최근 refs를 읽고 현재 상태를 먼저 보고
2. **기획** — `02_만들기.md` 1단계. 장면표를 만들어 합의부터
3. **장표** — `장표_템플릿.html` 스타일 그대로, 내용만 교체. `output/`에 저장
4. **이미지** — refs·assets의 실제 사진 우선 → 없으면 `assets/이미지_스타일.md` 규격으로 생성
   → 그것도 안 되면 점선 플레이스홀더
5. **영상** — `04_영상.md`대로 장면.json을 만들고 `scripts/make_video.py` 실행
6. **검사** — `03_검사.md`대로 렌더·PDF 쪽수·오디오를 검사하고 결과를 표로 보고

## 일하는 규칙

1. 사실의 근거는 refs의 파일뿐이다. 자료에 없는 숫자·이름·날짜·원인을 만들지 않는다
2. 근거가 없으면 지어내지 말고 `확인 필요`로 표시한다
3. 새 기록이 기존 결정과 충돌하면 덮어쓰지 말고 사람에게 보고한다
4. 만들기 전에 계획을 보여주고 합의한 뒤 만든다
5. 외부 발송·배포·삭제는 하지 않는다. 초안까지만 만들고 사람 승인을 기다린다
6. 이미지를 생성했으면 **직접 열어 확인**한다 — 글자 비슷한 것이 보이면 재생성

## 금지 (사용자가 채우세요)

- [고객 실명·연락처를 결과물에 넣지 않는다]
- [API 키·비밀번호를 어떤 파일에도 적지 않는다]

## 출력 기본값

한국어 존댓말 · 장표 16:9 · 영상 1280×720 MP4 · 문서는 마크다운
