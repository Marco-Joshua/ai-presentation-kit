# 04 · 영상 — 설치부터 MP4 검사까지

## 사용자는 명령어를 외울 필요가 없습니다

AI에게 이렇게 말하면 됩니다.

> `$project-briefing-video`를 사용해 이 프로젝트에 필요한 영상·음성 라이브러리를 설치하고, refs의 최신 기록으로 30초 주간보고 영상을 만들어줘. 필요한 이미지는 직접 만들고 MP4까지 검사해줘.

AI는 아래 작업을 끝까지 수행해야 합니다.

1. `bash scripts/bootstrap.sh`
2. 원문을 읽어 `manifest.json` 작성
3. 실제 이미지 선택 또는 이미지 생성
4. Crier/Supertonic F2로 장면별 WAV 생성
5. Remotion으로 1920×1080 동적 MP4 렌더
6. 오디오·해상도·길이 검사
7. 모든 장면 대표 프레임과 콘택트시트 확인

## 직접 실행할 때

```bash
bash scripts/bootstrap.sh
python3 scripts/render_video.py examples/customer-support-weekly-20260827/manifest.json \
  --output output/customer-support-weekly-20260827.mp4
```

`render_video.py`는 설치되지 않은 런타임이 있으면 자동으로 설치를 시도합니다.

## 음성

- 엔진: Crier와 동일한 Supertonic 로컬 ONNX 음성
- 기본: F2 여성
- 네트워크 API와 API 키: 불필요
- 형식: 장면별 WAV 생성 후 영상에 합성
- 한국어 숫자: Crier의 한국어 정규화 적용
- 외부 서비스명: 대본에서 한글 발음으로 치환

브라우저 `speechSynthesis`, macOS `say`, 품질이 다른 TTS로 자동 대체하지 않습니다. Supertonic 설치 또는 실행이 실패하면 원인을 알리고 중단합니다.

음성을 바꾸려면 manifest의 값을 수정합니다.

```json
{
  "voice": {"id": "F2", "speed": 1.08, "steps": 8}
}
```

F1~F5는 여성, M1~M5는 남성 음성입니다.

## 음악

주간보고는 기본적으로 BGM을 넣지 않습니다. 온보딩·인트로처럼 음악이 필요한 경우에만 manifest에 추가합니다.

```json
{
  "bgm": "assets/bgm_soft_circuit_drift.mp3",
  "bgmVolume": 0.07
}
```
