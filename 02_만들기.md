# 02 · 만들기 — 요청을 결과물로 바꿉니다

## 요청이 형식을 결정합니다

`PROJECT_STATE.md`와 예제는 사실의 근거입니다. 사용자가 이번에 무엇을 만들라고 했는지는 가장 최근 대화에서 결정합니다.

예를 들어:

> 8월 14일 기록으로 30초 주간보고 영상을 만들어줘.

이 요청을 온보딩 안내 영상으로 바꾸지 않습니다. 내용이 온보딩 프로젝트에 관한 것이라도 **산출물은 온보딩 프로젝트의 주간보고 영상**입니다.

## 기획 원칙

장면은 다음 질문에 차례로 답해야 합니다.

1. 무엇을 보고하는가?
2. 어떤 원문을 함께 읽었는가?
3. 이번에 무엇이 결정됐는가?
4. 충돌하거나 확인할 것은 무엇인가?
5. 다음 행동은 무엇인가?

한 장면에는 메시지 하나만 둡니다. 자료가 적다면 억지 통계나 성과를 만들지 말고 결정·충돌·다음 행동을 시각화합니다.

## 같은 맥락에서 결과물만 바꾸는 요청

새로 설명하지 말고, 이미 읽은 `PROJECT_STATE.md`와 `refs/`를 같은 근거로 사용합니다.

### 슬랙 일일보고 초안

```text
오늘 refs에 추가된 기록과 현재 상태를 근거로 슬랙에 붙여넣을 [오늘 한 일] 초안을 써줘.
프로젝트별로 완료·진행·확인 필요를 나누고, 하지 않은 일은 넣지 마.
슬랙으로 보내지 말고 output/daily-report.md에 초안만 저장해줘.
```

### 주간보고 장표와 PDF

```text
이번 주 refs와 PROJECT_STATE.md를 근거로 주간보고를 만들어줘.
핵심 결정·진행 상태·주요 수치·위험·다음 주 행동 순서로 16:9 HTML 장표를 만들고 PDF로도 내보내줘.
실제 스크린샷과 사진을 우선하고, 모든 장표를 렌더해 잘림·겹침·폰트·페이지 수를 검사해줘.
```

### 발표 대본과 브리핑 영상

```text
방금 만든 주간보고를 30초 브리핑 영상으로 만들어줘.
먼저 장면별 대본과 화면을 확정하고, 화면·내레이션·자막이 같은 내용을 같은 순서로 보여주게 만들어줘.
실제 이미지를 우선하고, 필요한 이미지는 장면 비율에 맞게 만들어줘.
배경음악은 넣지 말고 무료 로컬 한국어 음성을 사용해 MP4까지 렌더한 뒤 음성·해상도·대표 프레임을 검사해줘.
```

## 장표 제작

```text
refs와 PROJECT_STATE.md를 근거로 이번 요청에 맞는 16:9 발표 자료를 만들어 주세요.

- 사용자의 최신 요청이 산출물 형식과 길이를 결정합니다.
- 장표_템플릿.html의 큰 타이포, 여백, 이미지 중심 구성을 따릅니다.
- 제목은 70px 이상, 본문은 26px 이상으로 만듭니다.
- 한 장표에 메시지 하나만 둡니다.
- 실제 스크린샷과 사진을 먼저 크게 사용합니다.
- 이미지가 없고 Codex 이미지 생성이 가능하면 필요한 구도에 맞춰 에셋을 생성합니다.
- 카드 격자와 작은 글씨로 화면을 채우지 않습니다.
- HTML과 PDF를 output에 저장하고 모든 장표를 렌더해 잘림과 겹침을 검사합니다.
```

HTML을 만든 뒤 PDF와 전 장표 콘택트시트는 다음 명령으로 생성합니다.

```bash
python3 scripts/export_deck.py output/deck.html --pdf output/deck.pdf
```

## 동적 영상 제작

`examples/weekly-report-20260814/manifest.json`을 복사해 이번 내용으로 바꿉니다. 사용할 수 있는 장면 유형은 다음과 같습니다.

- `hero`: 표지와 핵심 메시지
- `sources`: 메일·통화·회의·메신저 등 근거 출처
- `list`: 완료 사항·결정·다음 행동
- `compare`: 변경 전후 또는 서로 충돌하는 두 정보
- `closing`: 결론과 다음 단계

내레이션과 화면의 순서는 일치해야 합니다. 대본에서 외부 서비스명은 `노션`, `슬랙`, `깃허브`처럼 한글로 씁니다.

완성 명령:

```bash
python3 scripts/render_video.py output/manifest.json --output output/final.mp4
```

필요한 라이브러리가 없으면 렌더러가 `scripts/bootstrap.sh`를 실행해 설치합니다.

## 품질이 부족할 때

“더 예쁘게”가 아니라 관찰 가능한 문제를 고칩니다.

```text
현재 결과는 삭제하지 말고 새 파일로 저장해 주세요.
- 화면과 내레이션이 같은 내용을 같은 순서로 보여주게 수정
- 제목은 최소 70px, 핵심 수치는 화면의 주인공으로 확대
- 실제 이미지나 생성 이미지가 화면의 절반 이상을 차지하게 구성
- 카드형 UI를 줄이고 평면적인 타이포 구성으로 수정
- 장면 내부에 스프링 진입, 숫자 변화, 선 리빌, 카메라 이동 중 필요한 것만 적용
- 수정 후 시작·중간·끝과 모든 장면 전환 프레임을 다시 검사
```
