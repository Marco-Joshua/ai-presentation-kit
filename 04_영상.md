# 04 · 영상 — 장표에 목소리와 자막을 붙입니다

> 장표(HTML)가 완성된 뒤의 단계입니다. 유료 TTS 없이, 무료 뉴럴 보이스로 만듭니다.

## 준비 (처음 한 번)

```
pip3 install supertonic      # 무료 · 로컬 고품질 TTS (권장 · 여성 F1~F5 / 남성 M1~M5)
brew install ffmpeg          # 윈도우: winget install ffmpeg
```

Supertonic 설치가 어려우면 대신 `pip3 install edge-tts`(온라인 보이스)로도 동작합니다.
스크립트가 Supertonic이 있으면 자동으로 그쪽을 씁니다.

## 1단계 — AI에게 장면 대사를 만들게 합니다

```
합의한 장면표를 바탕으로 장면.json을 만들어 주세요.

- 형식: [{"slide": 1, "text": "..."}, ...]
- 대사는 화면에 보이는 내용과 같은 말을 해야 합니다. 화면 따로 말 따로면 안 됩니다.
- 한 장면 대사는 한두 문장. 숫자·이름은 원자료에 있는 것만.
- 다 만들면 각 대사를 소리 내어 읽었을 때 어색한 곳을 스스로 표시해 주세요.
```

## 2단계 — 목소리를 고르고 만듭니다

```
python3 scripts/make_video.py --voices              # 목소리 목록
python3 scripts/make_video.py 장표.html 장면.json    # 기본(SunHi·여성)
python3 scripts/make_video.py 장표.html 장면.json --voice ko-KR-InJoonNeural
```

각 장면은 내레이션 길이에 맞춰 자동으로 넘어가고, 자막이 함께 박힙니다.

**배경음악**: `assets/`에 `bgm`으로 시작하는 mp3가 있으면 자동으로 깔립니다
(내레이션 아래 작게 · 끝에서 페이드아웃). 끄려면 `--no-bgm`, 다른 곡은 `--bgm 경로`.
키트에는 저작권 걱정 없는 곡이 한 개 들어 있습니다.

## 3단계 — 반드시 직접 확인합니다

- 영상을 열어 **소리가 나는지** 확인합니다 (무음인 채 완성되는 사고가 실제로 있습니다)
- 시작·중간·끝을 건너뛰며 화면과 대사가 맞는지 봅니다
- 발음이 어색한 단어는 대사를 바꾸는 게 빠릅니다 (예: "AX" → "에이엑스")

## 더 좋은 목소리가 필요하면

- **ElevenLabs** — 내 목소리 클론. 유료. 스크립트의 edge-tts 부분만 바꾸면 됩니다
- **macOS 내장** — `say -v Yuna` (인터넷 없이 동작, 품질은 낮음)
